<%= image_tag @pin.image.url(:medium) %>

<p>
  <strong>Locationbuilding:</strong>
  <%= @pin.locationBuilding %>
</p>

<p>
  <strong>Concern:</strong>
  <%= @pin.concern %>
</p>

<% if @pin.user == current_user %>
	<%= link_to 'Edit', edit_pin_path(@pin) %> |
<% end %>
<%= link_to 'Back', pins_path %>
