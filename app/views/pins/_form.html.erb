<%= form_for @pin, html: {multipart: true } do |f| %>
  <% if @pin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
      <% @pin.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



   <div class="form-group">
    <%= f.label "Church Name" %>
    <%= f.text_field :locationBuilding, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label "Clergy Name" %>
    <%= f.text_field :clergyname, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label "Comment" %>
    <%= f.text_field :concern, as: :text, text_field_html: { rows: "3" }, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </br>
</br>
    <p>"Don't use the force, let it load." #fixthiscampus</p>
  </div>

<% end %>
